import React from "react";

interface Props {}

const WorkoutVisualizer: React.FC<Props> = (props: Props) => {
  return (
    <div>
      <h1> Visualization Portion</h1>
    </div>
  );
};

export default WorkoutVisualizer;