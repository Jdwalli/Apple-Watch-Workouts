import React from "react";

interface Props {}

const WorkoutControl: React.FC<Props> = (props: Props) => {
  return (
    <div>
      <h1> Control portion</h1>
    </div>
  );
};

export default WorkoutControl;